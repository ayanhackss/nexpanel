<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>System Logs - NexPanel</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <style>
        body { font-family: 'Inter', sans-serif; background: #0F172A; color: #F1F5F9; margin: 0; display: flex; min-height: 100vh; }
        .sidebar { width: 260px; background: #1E293B; border-right: 1px solid rgba(255,255,255,0.05); display: flex; flex-direction: column; }
        .main-content { flex: 1; padding: 32px; overflow-y: auto; height: 100vh; box-sizing: border-box; display: flex; flex-direction: column; }
        .card { background: #1E293B; border: 1px solid rgba(255,255,255,0.05); border-radius: 12px; display: flex; flex-direction: column; flex: 1; overflow: hidden; }
        
        .log-header { padding: 16px 24px; border-bottom: 1px solid rgba(255,255,255,0.05); display: flex; justify-content: space-between; align-items: center; background: #1E293B; }
        
        .log-viewer { flex: 1; background: #0F172A; padding: 24px; overflow: auto; font-family: 'JetBrains Mono', monospace; font-size: 13px; color: #94A3B8; white-space: pre-wrap; line-height: 1.5; }
        
        select { padding: 8px 12px; background: #334155; border: none; border-radius: 6px; color: white; font-family: 'Inter', sans-serif; cursor: pointer; }
        .btn-refresh { background: #334155; border: none; color: white; padding: 8px 12px; border-radius: 6px; cursor: pointer; }
        .btn-refresh:hover { background: #475569; }
    </style>
</head>
<body>
    <%- include('partials/sidebar', { active: 'logs' }) %>

    <div class="main-content">
        <h1 style="font-size: 24px; font-weight: 700; margin-bottom: 8px;">System Logs</h1>
        <p style="color: #94A3B8; margin-bottom: 24px;">View server access and error logs.</p>

        <div class="card">
            <div class="log-header">
                <select id="logSelect" onchange="loadLog()">
                    <option value="nginx_error">Nginx Error Log</option>
                    <option value="nginx_access">Nginx Access Log</option>
                    <option value="syslog">System Log (Syslog)</option>
                    <option value="auth">Auth Log</option>
                    <option value="panel">Panel Log</option>
                </select>
                <button class="btn-refresh" onclick="loadLog()">Refresh Log</button>
            </div>
            <div id="logContent" class="log-viewer">Select a log file to view...</div>
        </div>
    </div>

    <script>
        async function loadLog() {
            const key = document.getElementById('logSelect').value;
            const viewer = document.getElementById('logContent');
            viewer.innerText = 'Loading...';

            try {
                const res = await axios.get(`/logs/api/view/${key}`);
                const content = res.data.content;
                viewer.innerText = content || '(Log file is empty)';
                viewer.scrollTop = viewer.scrollHeight; // Auto-scroll to bottom
            } catch (err) {
                viewer.innerText = 'Failed to load log: ' + (err.response?.data?.error || err.message);
            }
        }

        loadLog();
    </script>
</body>
</html>
